import 'package:flutter/material.dart';

class Catogaryscreen extends StatelessWidget {
  const Catogaryscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Catogary Screen"),),
    );
  }
}
